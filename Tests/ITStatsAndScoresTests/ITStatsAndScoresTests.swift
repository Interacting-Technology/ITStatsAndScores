import XCTest
@testable import ITStatsAndScores

final class ITStatsAndScoresTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(ITStatsAndScores().text, "Hello, World!")
    }
}
